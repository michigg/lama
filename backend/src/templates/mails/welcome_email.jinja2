<h1>Willkommen bei den %Firmenname% Services, {{ user.username }}!</h1>
<p>Es wurde ein Account für Sie angelegt.</p>
<p><strong>Ihr Nutzername lautet:</strong> {{ user.username }}</p>
<p><strong>Uns ist folgende E-Mail bekannt:</strong> {{ email }}</p>
<p>Über den folgenden Link können Sie Ihren Account aktivieren:
    <a href="{{ protocol }}://{{ domain }}{{ url('ldap_password_reset_confirm',
    kwargs={'uidb64':uid, 'token':token}) }}">
        {{ protocol }}://{{ domain }}{{ url('ldap_password_reset_confirm', kwargs={'uidb64':uid, 'token':token}) }}#
    </a>
</p>
<p>Der Link ist nur <strong>{{ expiration_days }} Tage</strong> gültig!</p>
<p>Und so geht's zum <a href="{{ protocol }}://{{ domain }}{{ url('login') }}">Login</a></p>