// https://docs.cypress.io/api/introduction/api.html

describe('Add Realm Test', () => {
  beforeEach(() => {
    cy.visit('/login')
  })
  it('elements exists', () => {
  })
  it('test mail positive feedback', () => {
  })
  it('test mail negative feedback', () => {
  })
})
